spring:
  application:
    name: cloud-monitor
  security:
    basic:
      enabled: true
    user:
      name: cloud-admin
      password: ${REGISTRY_SERVER_PASSWORD:cloud-admin}
  profiles:
    active: dev
